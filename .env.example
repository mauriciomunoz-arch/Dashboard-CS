# API key PERSONAL de Linear (lin_api_...). Vive SOLO server-side (Route
# Handlers), nunca llega al navegador ni a git. Cópiala en .env.local.
# Genérala/rótala en: Linear -> Settings -> Security & access -> API.
LINEAR_API_KEY=
